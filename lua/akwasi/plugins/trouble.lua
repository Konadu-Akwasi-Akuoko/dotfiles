return {
	"folke/trouble.nvim",
	dependencies = { "nvim-tree/nvim-web-devicons" },

	-- Your custom options, such as `focus = true`, are preserved.
	opts = {
		focus = true,
	},
	-- The `cmd` trigger ensures the plugin is loaded only when needed.
	cmd = "Trouble",
}
